# AI 找房助手 TODO

## 当前阶段

目标：完成内部客服可试用的 AI 找房助手 MVP。

MVP 核心闭环：

```text
客服输入需求
-> AI 抽取结构化需求
-> 后端通过 MCP 查询房源
-> 按规则扩圈和排序
-> 返回推荐房源
-> 生成客服可复制话术
-> 记录反馈
```

## P0：MVP 必做

- [ ] 确认 MVP 技术栈：前端、后端、模型服务、部署方式。
- [ ] 确认模型供应商：国内模型、OpenAI、公司内部模型网关，或多 Provider 抽象。
- [ ] 设计 `LLMProvider` 接口，支持后续替换模型。
- [ ] 设计模型结构化输出 schema：需求抽取、检索计划、推荐结果、客服话术。
- [ ] 实现模型输出校验：JSON 解析、字段校验、置信度判断、失败重试。
- [ ] 设计 AI 后端接口：`POST /api/ai-house-assistant/chat`。
- [ ] 设计会话数据结构：session、message、recommendation、feedback。
- [ ] 实现 MCP Client，支持 HTTP JSON-RPC 调用。
- [ ] 接入现有 MCP 工具：`search_houses`、`search_buildings`、`get_house_type_summary`。
- [ ] 修复或规避 `get_house_detail` 依赖 `house_images` 表导致失败的问题。
- [ ] 确认楼栋表是否稳定提供 `lng` 和 `lat`。
- [ ] 建立最小位置字典：广州高频地铁站、商圈、片区、重点楼栋。
- [ ] 实现最小位置解析：客户位置 -> 标准地点 -> 坐标 -> 置信度。
- [ ] 实现楼栋坐标距离排序。
- [ ] 实现需求抽取：位置、预算、户型、整租/合租、面积、朝向、入住时间。
- [ ] 实现预算规则：例如 `1000 左右` 默认转为 `800-1200`。
- [ ] 实现位置降级策略：严格位置 -> 周边距离 -> 地铁/商圈 -> 行政区。
- [ ] 实现房源排序：位置、预算、户型、空置、面积、更新时间。
- [ ] 实现推荐结果结构化输出：房源卡片、推荐原因、风险提示。
- [ ] 实现客服话术生成。
- [ ] 实现前端聊天界面。
- [ ] 实现推荐房源卡片。
- [ ] 实现一键复制客服话术。
- [ ] 实现客服反馈：合适、不合适、已复制、客户要看房。
- [ ] 记录完整调用日志：原始需求、结构化需求、MCP 参数、推荐结果、话术、反馈。
- [ ] 实现事件埋点：发送需求、位置解析、MCP 调用、推荐展示、话术复制、反馈提交。
- [ ] 确保 MCP Token 和模型 API Key 只保存在服务端。

## P1：位置能力

- [ ] 补充楼栋位置字段：行政区、街道、商圈、最近地铁站、距离地铁。
- [ ] 新增 MCP 工具 `resolve_location`。
- [ ] 新增 MCP 工具 `search_houses_geo`。
- [ ] 扩充广州常用地铁站、商圈、片区的位置字典。
- [ ] 实现地铁站扩圈：本站 -> 前后 1-3 站 -> 低换乘区域。
- [ ] 实现商圈扩圈：商圈中心 -> 相邻商圈 -> 同街道/同板块。
- [ ] 无法识别位置时，让 AI 追问而不是猜。

## P1：Skill 与规则

- [ ] 建立 `Search Policy Skill`：需求抽取、位置识别、检索顺序、无结果放宽、推荐理由。
- [ ] 建立 `Sales Reply Skill`：客服话术、销冠沟通结构、推进动作。
- [ ] 将硬规则放入配置文件，例如预算区间、推荐数量、MCP 调用上限。
- [ ] 为每个 Skill 准备 5-10 条输入输出样例。
- [ ] 沉淀销冠话术样例，但只提炼沟通结构，不直接复制个人口头禅。
- [ ] 灰度后按失败案例决定是否拆分 `Requirement`、`Location`、`Search Strategy`、`Recommendation` 等细分 Skill。

## P1：评测与灰度

- [ ] 收集 30-50 条真实客服找房需求作为评测集。
- [ ] 标注每条需求的正确结构化结果。
- [ ] 标注每条需求的可接受推荐结果或合理无房解释。
- [ ] 评测需求抽取准确率。
- [ ] 评测推荐采纳率。
- [ ] 评测话术复制率。
- [ ] 评测 MCP 调用失败率和平均响应时延。
- [ ] 灰度给 3-5 名客服使用。
- [ ] 每周复盘失败案例，更新规则和 Skill。

## P2：后续增强

- [ ] 嵌入现有客服后台。
- [ ] 接入地图展示。
- [ ] 支持客户偏好记忆。
- [ ] 支持看房预约辅助。
- [ ] 支持多城市、多项目策略。
- [ ] 建立销冠话术库和 A/B 测试。
- [ ] 基于客服反馈优化排序权重。
- [ ] 建立异常监控和告警。

## 当前已知问题

- `get_house_detail` 当前查询图片时会依赖 `house_images` 表；如果该表不存在，详情接口会失败。
- 当前 `search_houses` 主要是关键词查询，无法可靠表达“东平附近”“地铁前后几站”等位置需求。
- 纯关键词降级会把“东平”过快扩大到“白云区”，需要地理化位置策略。
- 模型工具调用能力可能因供应商不同而不稳定，MVP 应由后端控制 MCP 调用。
